@model IEnumerable<RegistroClientes.Models.Registros>

@{
    ViewBag.Title = "Inicio";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Inicio</h2>


<div class="row" style="margin-top: 30px">

    <div class="col-sm-12">
        <div class="panel panel-warning">
            <div class="panel-heading">
                <h4>Lista de contactos</h4>
            </div>
            <div class="panel-body">
                <a class="btn bnt-seccess" href="@Url.Action("registrarcliente","Registros")">Crear nuevo</a>
                <hr />
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Cedula</th>
                                <th>Nombre</th>
                                <th>Apellidos</th>
                                <th style="width" 150px></th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (RegistroClientes.Models.Registros item in Model)
                            {

                                <tr>
                                    <td>@item.Cedula</td>
                                    <td>@item.Nombre</td>
                                    <td>@item.Apellidos</td>
                                    <td>

                                        <a class="btn bnt-primary btn-sm" style="background-color: lawngreen; color: white; font: bold">Editar</a>
                                        <a class="btn bnt-danger btn-sm" style="background-color: red; color: white; font: bold" ;>Eliminar</a>

                                    </td>
                                </tr>

                            }

                        </tbody>
                    </table>

                </div>
            </div>
        </div>
    </div>

</div>